
# Oracle datasource - use service-name style (recommended for PDBs)
spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/FREEPDB1
spring.datasource.username=system
spring.datasource.password=swamy
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Server port
server.port=8080

# Static resources served from src/main/resources/static (frontend)
[new_code]

# --- PhonePe / UPI payment configuration (placeholders) ---
# Replace these with real merchant credentials when available.
payment.phonepe.merchantId=YOUR_MERCHANT_ID
payment.phonepe.merchantKey=YOUR_MERCHANT_SECRET_KEY
payment.phonepe.callbackUrl=http://localhost:8080/api/payments/phonepe/webhook
payment.merchant.upi=7625029626@icici

